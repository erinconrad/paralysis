function build_kappa_table(curr_method)

cats = unique(ciur
ncats = length(unique(curr_method));
nreviewers = size(curr_method,1);
neegs = size(curr_method,2);
kappa_table = nan(neegs,ncats);

for ie = 1:neegs
    for ic = 1:ncats
        kappa_table(ie,ic) = sum(curr_method(:,ie)==
    end
end



end